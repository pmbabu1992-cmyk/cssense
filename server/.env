# # --- Server ---
# PORT=5000
# NODE_ENV=development

# # --- MongoDB (Atlas) ---
# # NOTE: use the DB that has your data (e.g., /cns). Replace the credentials/host.
# MONGODB_URI="mongodb+srv://hello123:Mycompany%44623@abc.dddidux.mongodb.net/cns?retryWrites=true&w=majority"

# # --- CORS allow-list ---
# CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:4200,http://localhost:5173

# # --- Swagger ---
# # Hide in production by setting either SWAGGER_ENABLED=false, or NODE_ENV=production
# SWAGGER_ENABLED=true
# SWAGGER_SERVER_URL=http://localhost:5000



# --- Server ---
PORT=5000
NODE_ENV=development

# --- MongoDB (Atlas) ---
# Include your database name at the end of the URI (change "cssense" if you prefer)
# MONGODB_URI="mongodb+srv://hello123:Mycompany%44623@abc.dddidux.mongodb.net/cssense?retryWrites=true&w=majority"
MONGODB_URI=mongodb+srv://dineshj:Zhurion%40123@cns.ppbiuxc.mongodb.net/cns?retryWrites=true&w=majority"
# --- CORS allow-list (add/remove front-end origins you use) ---
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:4200,http://localhost:5173

# --- Swagger ---
# Keep Swagger on in dev
SWAGGER_ENABLED=true
# The base URL Swagger advertises (use your API URL in prod)
SWAGGER_SERVER_URL=http://localhost:5000




# In production you need to enable below

# PORT=8080
# NODE_ENV=production

# # Use the same cluster, but ensure the password is correctly URL-encoded and the DB name is present
# # MONGODB_URI="mongodb+srv://hello123:Mycompany%44623@abc.dddidux.mongodb.net/cssense?retryWrites=true&w=majority"
# MONGODB_URI=mongodb+srv://dineshj:Zhurion%40123@cns.ppbiuxc.mongodb.net
# # Only your real front-end/domain(s)
# CORS_ORIGINS=https://your-frontend.example

# # Hide Swagger in production
# SWAGGER_ENABLED=false
# SWAGGER_SERVER_URL=https://api.your-backend.example


# Note: Control docs via .env:

# Dev: NODE_ENV=development, SWAGGER_ENABLED=true → docs visible

# Prod: NODE_ENV=production or SWAGGER_ENABLED=false → docs hidden